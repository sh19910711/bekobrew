name: package-name
version: 1.2.3

sources:
  - https://example.com/packages/$name-$version.tar.gz

sums:
  - SHA-256

# comment
build:
  - "echo build: $name"
  - "echo build: $version"

package:
  - "echo package: $name"
  - "echo package: $version"
